#ifndef __MODE_H__
#define __MODE_H__

/**
 * @brief Mode enum
 *
 * 该枚举类型定义了主菜单的模式
 */
typedef enum {
    MAIN = 0,
    CREATE,
    OUTPUT,
    QUERY,
    UPDATE,
    DELETE,
    EXIT,
} Mode;

#endif
